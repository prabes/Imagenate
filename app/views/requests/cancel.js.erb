<%#= $('#followbutton').addEventListener('ajax:success', event => {
  const [response, status, xhr] = event.detail;
  console.log("Here is the response: ", response);
}); %>
$('#followbutton<%=params[:user_id]%>').replaceWith("<%= j render partial: 'layouts/followbutton', locals: {user: User.find(params[:user_id])} %>");