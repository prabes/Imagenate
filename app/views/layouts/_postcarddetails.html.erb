<div class="post-details card-title">
  <%if post.user.profile.image.length > 0 %> 
    <%=image_tag(post.user.profile.image.last.image.url(:thumb), size: '40x40', class: "rounded-circle")%>
  <%end%>
  <div class="pt-1 pl-1" > <strong> <%=post.user&.profile&.username%> </strong>
    <span> <%=post.description%>
  </div>
</div>